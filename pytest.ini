[pytest]
testpaths = tests
filterwarnings =
    # We deliberately use paho's v1 callback API: it is the only one that
    # works on both paho-mqtt 1.x (what distros ship) and 2.x.
    ignore:Callback API version 1 is deprecated:DeprecationWarning
